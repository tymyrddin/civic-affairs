# Quiet Room environment — copy to .env and fill in values. Never commit .env.

# Network interface for packet capture (e.g. eth0, ens3, bond0)
SENSOR_INTERFACE=eth0

# Wazuh API user (set matching values in ossec.conf)
WAZUH_API_USER=wazuh-wui
WAZUH_API_PASSWORD=changeme-strong-password
