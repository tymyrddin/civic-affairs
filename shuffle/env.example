# Shuffle environment — copy to .env and fill in values. Never commit .env.
# Run ./ctl init to generate secrets automatically.

# API key used to authenticate internal Shuffle components.
# Must be a random string of 32 or more characters.
SHUFFLE_BACKEND_APIKEY=changeme-shuffle-api-key
