# Shuffle environment — copy to .env and fill in values. Never commit .env.

# Backend API authentication key (use a random string of 32+ characters)
SHUFFLE_BACKEND_APIKEY=changeme-replace-with-random-32-char-string
